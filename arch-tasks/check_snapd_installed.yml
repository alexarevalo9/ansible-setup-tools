- name: Snap
  become: true
  tags: "snap"
  block:
    - name: Install snapd
      shell: yay -S snapd --noconfirm

    - name: Check if snapd is installed
      community.general.pacman:
        name: snapd
        state: present
        executable: yay
      register: snapd_check
